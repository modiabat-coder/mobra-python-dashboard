# Copy these settings to Streamlit Community Cloud > App settings > Secrets,
# or to a local .streamlit/secrets.toml file that is never committed.
[auth]
enabled = true
username = "replace-with-your-username"
password_hash = "replace-with-generated-pbkdf2-hash"
session_timeout_minutes = 60
