-r requirements.txt

# Keep the test runner explicit here so a development/CI install remains
# self-documenting even if runtime dependencies are reorganized later.
pytest>=8.0,<10
black>=24.10,<27
hypothesis>=6.100,<7
pytest-cov>=5,<8
ruff>=0.8,<1
